#include <bits/stdc++.h>
using namespace std;
int main()
{   
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;
    while (t--)
    {

        int N,D;
        cin >> N >> D;

        vector<int> power(N);
        for(auto &x : power){
                cin >> x;
            }

           sort(power.begin(),power.end());
            
        


    }
}